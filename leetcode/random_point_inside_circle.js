const angle = Math.random() * 2 * Math.PI;
const radius = 2
const center_x=2
const center_y=2
const hypotenuse Math.random() * radius
const adjacent = Math.cos(angle) * hypotenuse
const opposite = Math.sin(angle) * hypotenuse
const point_x = center_x + adjacent
const point_y = center_y + opposite
